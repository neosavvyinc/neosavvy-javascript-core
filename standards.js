/* This js file is only intended as a starting point from the preferred code style in Neosavvy, Inc. application and library code */

//Camel case
var myInstance = {};
var MyClassOrNamespace = MyClassOrNamespace || {};

myInstance.myProperty = "This is a property";

//UI Event Handlers
var onMouseClick = function() {
    //do something
};
var onKeyUp = function() {
    //do something
};

//Boolean evaluator methods
var isVisible = function() {
    //return some visibility boolean
};
var isChecked = function() {
    //return some visibility boolean
};

//Asynchronous handlers (service calls, etc)
var handleSomeServiceSuccess = function(data) {
    //do something with data
};
var handleSomeServiceError = function(message) {
    //do something with error
};
